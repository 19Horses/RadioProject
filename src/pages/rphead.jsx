// Re-export components for backward compatibility
import RPHead from "../components/visitor/RPHead";
import { RPGrid } from "../components/visitor/RPGrid";

export default RPHead;
export { RPGrid };
