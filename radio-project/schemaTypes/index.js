import mix from '../../sanity/schemas/mix'
import radiogram from '../../sanity/schemas/radiogram'
import siteSettings from '../../sanity/schemas/siteSettings'

export const schemaTypes = [mix, radiogram, siteSettings]